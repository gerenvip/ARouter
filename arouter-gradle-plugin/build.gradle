apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

dependencies {
    compile 'com.android.tools.build:gradle:4.1.1'
    compile 'org.ow2.asm:asm:7.1'
    compile 'commons-io:commons-io:2.4'
}

//apply from: rootProject.file('gradle/publish.gradle')

if (file("${rootProject.projectDir}/maven_publish/mvn_publish_task.gradle").exists()) {
    apply from: "${rootProject.projectDir}/maven_publish/mvn_publish_task.gradle"
}